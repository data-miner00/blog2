<!doctype html>
<html lang="zh">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
      {{ title }}
    </title>

    <link rel="stylesheet" href="/styles/index.css">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
  </head>
  <body>
    <div class="mx-auto max-w-160">
      <header class="mt-10 mb-12">
        <div class="flex justify-between items-center">
          <a class="nonchalant " href="/">
            <h1 class="text-xl font-bold">Shaun Chong</h1>
          </a>
          <nav class="flex gap-4 items-center">
            <a href="/">主页</a>
            <a href="/posts">帖子</a>
            <a href="/feed.xml">订阅源</a>
          </nav>
        </div>
      </header>

      <div class="mb-8 prose">
        {{ content }}
      </div>

      {% assign previousPost = collections.post | getPreviousCollectionItem: page %}
      {% assign nextPost = collections.post | getNextCollectionItem: page %}
      {% if previousPost -%}
        <p>
          前: <a href="{{ previousPost.url }}">{{ previousPost.data.title }}</a>
        </p>
      {%- endif %}
      {% if nextPost -%}
        <p>
          后: <a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a>
        </p>
      {%- endif %}

      <footer class="mt-12 border-t border-solid border-gray-500 py-6">
        <div class="flex justify-between">
          <p>
            <em
              >搭建于 <a href="https://www.11ty.dev/">{{ eleventy.generator }}</a></em
            >
          </p>

          <p>
            &copy; {% year %}
            {{ metadata.author.name }}
          </p>
        </div>
      </footer>
    </div>
  </body>
</html>
